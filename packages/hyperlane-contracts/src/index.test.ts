import { describe, test, expect } from "vitest";

describe("Dummy Test (so vitest works)", function () {
    test("expect(true).toBe(true)", async () => {
        expect(true).toBe(true);
    });
});
